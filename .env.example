# Supabase Configuration
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_xxx

# Encryption Key for API Keys (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
# IMPORTANT: Keep this secret and never commit actual values to version control
ENCRYPTION_KEY=your-32-character-minimum-secret-key-here
